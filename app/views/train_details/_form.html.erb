<%= form_with(model: train_detail) do |form| %>
  <% if train_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(train_detail.errors.count, "error") %> prohibited this train_detail from being saved:</h2>

      <ul>
        <% train_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
